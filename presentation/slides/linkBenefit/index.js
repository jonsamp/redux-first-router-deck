import React from "react";
import { BlockQuote, Slide, Quote } from "spectacle";

export default (
  <Slide transition={["slide"]} bgColor="secondary">
    <BlockQuote>
      <Quote>✨ Links aren't coupled with their route paths</Quote>
    </BlockQuote>
  </Slide>
);
